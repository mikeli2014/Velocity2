# Velocity API — runtime deps
# Pinned to specific minor versions; loose patch range is fine via ~=.
fastapi~=0.115.0
uvicorn[standard]~=0.32.0
pydantic~=2.9.0
pydantic-settings~=2.6.0
SQLAlchemy~=2.0.36
# Postgres driver: only loaded when DATABASE_URL points at postgres://. SQLite
# (the dev default) uses the stdlib's sqlite3.
psycopg[binary]~=3.2.0
# Anthropic SDK + httpx are coupled: anthropic 0.39.x calls
# httpx.Client(proxies=...), but httpx 0.28+ removed that kwarg —
# leading to `TypeError: got an unexpected keyword argument 'proxies'`
# when Anthropic() is instantiated. Pin httpx to the 0.27.x line
# until we upgrade the SDK to a version that supports httpx 0.28+.
httpx>=0.27,<0.28
anthropic~=0.39.0
